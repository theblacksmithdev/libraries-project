// Simplified wrapper as the primary export
export { Alert, AlertPrimitives } from './alert.simple'
export type { AlertProps } from './alert.simple'

// Sub-parts still available individually (backward compat)
export { AlertTitle, AlertDescription } from './alert'
