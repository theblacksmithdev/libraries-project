// Simplified wrapper as the primary export
export { Tabs, TabsPrimitives } from './tabs.simple'
export type { TabsProps, TabDef } from './tabs.simple'

// Sub-parts still available individually (backward compat)
export { TabsList, TabsTrigger, TabsContent } from './tabs'
