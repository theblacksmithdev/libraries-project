// Simplified wrapper as the primary export
export { Popover, PopoverPrimitives } from './popover.simple'
export type { PopoverProps } from './popover.simple'

// Sub-parts still available individually (backward compat)
export { PopoverTrigger, PopoverContent, PopoverAnchor } from './popover'
