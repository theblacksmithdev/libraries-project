// Simplified wrapper as the primary export
export { HoverCard, HoverCardPrimitives } from './hover-card.simple'
export type { HoverCardProps } from './hover-card.simple'

// Sub-parts still available individually (backward compat)
export { HoverCardTrigger, HoverCardContent } from './hover-card'
